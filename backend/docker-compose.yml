services:
  backend-aivo:
    build: .
    container_name: api-container
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn backend:app --host 0.0.0.0 --port 8000